module.exports = {
    name: 'ligma',
    description: 'joe has ligma',
    //devOnly: Boolean,
    //testOnly: Boolean,
    //options: Object[],

    callback: (client, interaction) => {
        interaction.reply(`ligmaballs`);
    }
}